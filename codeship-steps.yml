# default
# -------
# always run the tests and lint the codebase
- name: default
  type: serial
  steps:
    #    - name: lint
    #      service: app
    #      command: yarn lint
    #    - name: test
    #      service: app
    #      command: yarn test
    - name: integration-test
      service: cypress
      command: npm run cy:run --env MOODLE_URL=moodle-web-server,APP_URL=app:3000
