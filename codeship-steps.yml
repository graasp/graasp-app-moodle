# default
# -------
# always run the tests and lint the codebase
- name: default
  type: serial
  steps:
    - name: lint
      service: app
      command: yarn lint
    - name: test
      service: app
      command: yarn test
    - name: integration-test
      service: data-aggregation-module
      command: yarn try:ssat
