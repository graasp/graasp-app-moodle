# default
# -------
# always run the tests and lint the codebase
- name: default
  type: serial
  steps:
    #    - name: lint
    #      service: app
    #      command: yarn lint
    #    - name: test
    #      service: app
    #      command: yarn test
    - name: integration-test
      service: app
      # command: npx cypress run --config screenshotOnRunFailure=false,video=false --env APP_URL=http://app:3000
      command: yarn try:ssat
