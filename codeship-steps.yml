# default
# -------
# always run the tests and lint the codebase
- name: default
  type: serial
  steps:
    #    - name: lint
    #      service: app
    #      command: yarn lint
    #    - name: test
    #      service: app
    #      command: yarn test
    - name: integration-test
      service: app
      type: parallel
      steps:
        - command: yarn start:ci
        - command: 'sleep 60 && yarn run cy:run'
